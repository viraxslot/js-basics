// js basics 3: modules (require/import)
import { calc } from './module.js';
import { testData } from './test-data.js';
// const _ = require('lodash');

console.log(calc(testData.test));

const arr = [1, 2, 3, 4];
// const res = _.filter(arr, (el) => el > 2);
// console.log(res);
